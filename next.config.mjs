/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    reactStrictMode: 'true',
    images: {
        unoptimized: true
    },
    env: {}
};

export default nextConfig;
